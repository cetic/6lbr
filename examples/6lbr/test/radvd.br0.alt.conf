interface br0
{
        AdvSendAdvert on;

        prefix aaaa::0/64
	{
		AdvValidLifetime 0;
		DeprecatePrefix on;
                AdvOnLink on;
                AdvAutonomous on;
        };

        prefix 9999::0/64
	{
                AdvOnLink on;
                AdvAutonomous on;
        };

};
